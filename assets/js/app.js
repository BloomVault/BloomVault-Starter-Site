// Placeholder JS for BloomVault Farms
document.addEventListener('DOMContentLoaded', () => {
  const featured = document.getElementById('featured');
  if (featured) {
    featured.innerHTML = '<div class="card">Placeholder strain</div>'.repeat(4);
  }
});
